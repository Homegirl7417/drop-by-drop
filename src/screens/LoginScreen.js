import React from 'react';
import Template from '../component/Template';

const LoginScreen = () => {
    return (
        <Template>
            로그인 페이지
        </Template>
    );
}

export default LoginScreen;