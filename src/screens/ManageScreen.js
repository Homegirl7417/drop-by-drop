import React from 'react';
import Template from '../component/Template';

const ManageScreen = () => {
    return (
        <Template>
            진행 작업 관리
        </Template>
    );
}

export default ManageScreen;